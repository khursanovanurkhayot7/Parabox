namespace Luxodd.Game.Scripts.Input
{
    // Compatibility placeholder kept at the previous asset path so upgrades and existing .meta
    // references remain stable. ArcadeControls now lives in Runtime/Core/Input, where the updated
    // Luxodd Input System assembly can reference it.
}
